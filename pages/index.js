import Head from "next/head";
import CategoryCart from "../components/category/CategoryCart";
import { Categories } from "./api/categories";

export default function Home({ data }) {
  return (
    <>
      <div className="w-full text-right px-1 mt-10 text-xl font-semibold text-red-500">
        دسته بندی
        {data.map((category) => (
          <CategoryCart key={category} data={category} />
        ))}
      </div>
      <div className="flex flex-col justify-center mx-auto w-full"></div>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
    </>
  );
}

// This gets called on every request
export async function getServerSideProps() {
  const result = await Categories();

  const data = result.data ? result.data.allCategory : [];

  // Pass data to the page via props
  return { props: { data } };
}
